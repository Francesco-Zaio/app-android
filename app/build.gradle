apply plugin: 'com.android.application'

android {
    signingConfigs {
        release {
            keyAlias 'mydoctorkeystore'
            keyPassword 'mydoctor'
            storeFile file('C:/Users/SO000228/Desktop/TMED/Sviluppo/GWAndroid/AndroidCore/myDoctor.keystore')
            storePassword 'mydoctor'
        }
    }
    compileSdkVersion 26
    buildToolsVersion "26.0.3"
    useLibrary 'org.apache.http.legacy'
    defaultConfig {
        applicationId "com.ti.app.mydoctor"
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 403
        versionName "4.0.3.a"
        setProperty("archivesBaseName", "NuvolaItHomeDoctor-$versionName")
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            debuggable true
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt')
        }
    }
    productFlavors {
    }
}

dependencies {
    compile project(':core')
    // debugCompile 'com.amitshekhar.android:debug-db:1.0.0'
}
